@model IEnumerable<Employee>
@{
    ViewBag.Title = "List Of Employees";
}

<h2 class="text-info">
    List Of Employees
</h2>
<hr />

<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Gender</th>
                <th>Salary</th>
            </tr>
        </thead>
        @foreach (var emp in Model)
        {
            <tbody>
                <tr>
                    <td>@emp.EmployeeId</td>
                    <td>@emp.Name</td>
                    <td>@emp.Email</td>
                    <td>@emp.Gender</td>
                    <td>@emp.Salary.Value.ToString("c")</td>
                    <td>
                        <a asp-action="Edit" asp-controller="Employee" class="btn btn-outline-dark mb-2" asp-route-id="@emp.EmployeeId">Edit</a>
                        <a asp-action="View" asp-controller="Employee" class="btn btn-outline-info mb-2" asp-route-id="@emp.EmployeeId">View</a>
                        <a asp-action="Delete" asp-controller="Employee" class="btn btn-outline-danger mb-2" asp-route-id="@emp.EmployeeId">Delate</a>
                    </td>
                </tr>

            </tbody>
        }

    </table>
</div>
